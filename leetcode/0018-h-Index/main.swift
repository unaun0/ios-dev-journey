// Дан массив целых чисел citations, 
// где citations[i] — это количество цитирований, 
// полученных исследователем для своей i-й статьи. 
// Необходимо определить h-индекс этого исследователя.
// Согласно определению h-индекса из Википедии:
// h-индекс — это максимальное значение h, такое, 
// что исследователь опубликовал как минимум h статей, 
// каждая из которых была процитирована как минимум h раз.

// Пример 1:
//
// Входные данные: citations = [3, 0, 6, 1, 5]
// Выходные данные: 3
//
// Объяснение:
// Исследователь имеет 5 статей с количеством цитирований: [3, 0, 6, 1, 5].
// 3 статьи имеют ≥ 3 цитирований (3, 6, 5).
// Остальные 2 статьи имеют ≤ 3 цитирований (0, 1).
// Следовательно, h-индекс = 3.
//
// Пример 2:
//
// Входные данные: citations = [1, 3, 1]
// Выходные данные: 1
// Объяснение:
// 1 статья имеет > 1 цитирование (3).
// Остальные 2 статьи имеют ≤ 1 цитирования (1, 1).
// h-индекс = 1 (так как нет 2 статей с ≥ 2 цитированиями).
//
// Ограничения:
//
// n == citations.length
// 1 <= n <= 5000
// 0 <= citations[i] <= 1000
// 🔍 Разбор задачи:

// H-индекс — это показатель продуктивности учёного, основанный на количестве публикаций и их цитирований.

// Условие:

// Найти максимальное число h, такое что:
// В массиве citations есть как минимум h элементов ≥ h.
// Остальные элементы ≤ h.

